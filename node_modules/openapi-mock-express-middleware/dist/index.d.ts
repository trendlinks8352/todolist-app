import express from 'express';
import type { OpenAPIV3 } from 'openapi-types';
import { JSFOptions, JSFCallback } from './utils';
export interface MiddlewareOptions {
    spec?: string | OpenAPIV3.Document;
    options?: Partial<JSFOptions & {
        randomizeExamples: boolean;
        sortPaths?: boolean;
    }>;
    configure?: JSFCallback;
}
export declare const createMockMiddleware: ({ spec, options, configure, }: MiddlewareOptions) => express.Router;
